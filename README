With the following procedures you can synchronize/replicate your
`mysql` schema into `mysql-$hostname` schema. So you can ignore
`mysql` schema in replication setting and safely replicate
`mysql-$hostname` schema.

GOAL: multimaster replication to single backup server

